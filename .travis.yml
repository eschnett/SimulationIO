notifications:
  email: false
os:
  - linux
  # - osx
sudo: false
# install:
#   - sudo apt-get update && sudo apt-get -y install g++-4.9 libhdf5-serial-dev
#   # - brew update && brew install libhdf5
addons:
  apt:
    packages:
      - libhdf5-serial-dev
language: cpp
compiler:
  # - clang
  - gcc
script:
  - make
  - make test
