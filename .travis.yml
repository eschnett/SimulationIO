notifications:
  email: false
os:
  - linux   # This is currently Ubuntu 12.04 LTS
  - osx
sudo: false
addons:
  apt:
    packages:
      - libhdf5-serial-dev
before_install:
  - test `uname` = Darwin && brew update
  - test `uname` = Darwin && brew install libhdf5
language: cpp
compiler:
  - clang
  - gcc
script:
  - make
  - make test
